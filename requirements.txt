# 核心依赖
torch>=2.1.0
torchvision
torchaudio
transformers>=4.40.0
accelerate>=0.30.0
bitsandbytes>=0.43.0
peft>=0.11.0
trl>=0.8.0

# 视觉模型
openai-clip
timm==0.6.7

# MobileSAM (需要从 GitHub 安装)
# 方法 1: 在 WSL 中直接安装 (需网络通)
#   pip install git+https://github.com/ChaoningZhang/MobileSAM.git
# 方法 2: Windows 下载后在 WSL 安装
#   Windows: git clone https://github.com/ChaoningZhang/MobileSAM.git
#   WSL:     cd /mnt/d/experiment/MobileSAM && pip install -e .
mobile_sam @ git+https://github.com/ChaoningZhang/MobileSAM.git

# 仿真环境
ai2thor>=5.0.0

# 数据处理
numpy
scipy
opencv-python
Pillow
PyYAML

# 评估与可视化
matplotlib
seaborn
tqdm

# 工具
tensorboard
# wandb              # 实验跟踪(可选)
